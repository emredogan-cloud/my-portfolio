/**
 * @emredogan/lumina-chat — public entry point.
 *
 * Stable public API surface (only export from here):
 *   - LuminaChat       — composed orchestrator (trigger + window)
 *   - LuminaWindow     — the chat panel itself
 *   - LuminaTrigger    — the dormant-core activator button
 *   - LuminaAvatar     — the cyan neural-core circle
 *   - types: LuminaChatProps, LuminaWindowProps, LuminaTriggerProps,
 *            LuminaAvatarProps, LuminaTheme
 *
 * Style import (consumers must include once):
 *   import "@emredogan/lumina-chat/styles.css";
 */

export {};
